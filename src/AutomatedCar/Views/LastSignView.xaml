<UserControl x:Class="AutomatedCar.Views.LastSignView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:AutomatedCar.ViewModels"
             xmlns:converters="clr-namespace:AutomatedCar.Converters"
             d:DataContext="{d:DesignInstance viewmodels:LastSignViewModel}"
             xmlns:local="clr-namespace:AutomatedCar.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Background="LightGray" HorizontalAlignment="Center" VerticalAlignment="Bottom" Grid.Column="0" Text="Last sign:"/>
        <Image HorizontalAlignment="Center" Grid.Row="1" Height="Auto" Width="Auto" Source="{Binding ResourcePath}"/>
    </Grid>
</UserControl>
